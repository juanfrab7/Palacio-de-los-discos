const CartWidget = () => {
    return(
        <div>
            {/* logo del carrito */}
            <span>🛒</span>
            {/* numero estático por el momento */}
            <span style={{color: 'red'}}>5</span>
        </div>
    )
}

export default CartWidget